This folder contains all agents that will be in this project.

- chatbot agent: Allow lookup for given candidates
- retriever agent: performs the vector search adn generates the summary for the chosen people.